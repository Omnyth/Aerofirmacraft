modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license}"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
authors="Omnyth"
description='''
Terrain prototype addon for Aerofirmacraft.

Initial milestone:
- load beside the pack
- log startup lifecycle events
- make no worldgen changes yet

Long-term goal:
Preserve normal TFC regions horizontally while transforming landmasses into floating terrain above a global ocean floor.
'''

[[dependencies.${mod_id}]]
modId="neoforge"
type="required"
versionRange="[${neo_version},)"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="minecraft"
type="required"
versionRange="${minecraft_version_range}"
ordering="NONE"
side="BOTH"
